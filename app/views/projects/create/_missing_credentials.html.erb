<div class="flex flex-col">
    <div class="p-10">
        <h1 class="text-2xl font-bold">Missing Credentials for <%= params[:provider].titleize %></h1>
        <p class="mt-2 text-gray-400">Please provide your <%= params[:provider].titleize %> credentials to continue.</p>
        <%= link_to "Add Credentials", providers_path, class: "mt-6 btn btn-primary", data: { turbo: false } %>
    </div>
</div>
